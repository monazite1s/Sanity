# 环境变量配置示例

# Sanity 项目配置
SANITY_STUDIO_PROJECT_ID=your-project-id
SANITY_STUDIO_DATASET=production

# Sanity API Token (用于同步脚本，需要 Write 权限)
# 获取方式：https://sanity.io/manage -> 选择项目 -> API -> Tokens -> Add API token
# 权限：Editor 或 Administrator
SANITY_API_TOKEN=your-write-token-here
