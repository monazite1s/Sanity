# 知识库文档源文件目录

本目录用于存放前端系统性知识总结的 Markdown 文档，文件结构将映射为 Sanity 中的多级目录结构，最终呈现为类似 VuePress/VitePress 的知识库系统。

## 📁 目录结构规范

### 基本结构示例

```bash
docs-source/
├── frontend-basics/          # 一级分类（目录）
│   ├── _meta.json           # 分类元数据（可选）
│   ├── html-css/            # 二级分类（子目录）
│   │   ├── _meta.json
│   │   ├── html5-new-features.md
│   │   └── css-layout.md
│   └── javascript/
│       ├── _meta.json
│       ├── es6-features.md
│       └── async-programming.md
├── react/
│   ├── _meta.json
│   ├── basics/
│   │   ├── jsx-syntax.md
│   │   └── components.md
│   ├── hooks/
│   │   ├── useState.md
│   │   ├── useEffect.md
│   │   └── custom-hooks.md
│   └── advanced/
│       ├── performance.md
│       └── patterns.md
└── typescript/
    ├── _meta.json
    ├── basic-types.md
    └── advanced-types.md
```

### 组织原则

1. **使用目录表示分类**：每个目录对应知识库的一个分类节点
2. **支持无限嵌套**：可以创建任意层级的子分类
3. **文档必须在分类下**：根目录的 `.md` 文件会被同步脚本跳过
4. **按数字前缀排序**：建议使用 `01-`, `02-` 等前缀控制显示顺序

## 📄 元数据文件 `_meta.json`

每个分类目录可以包含一个 `_meta.json` 文件来定义元数据：

```json
{
  "title": "React 基础",
  "slug": "react-basics",
  "order": 1
}
```

### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `title` | string | 否 | 分类显示名称，未设置则使用目录名 |
| `slug` | string | 否 | URL 路径片段，未设置则使用目录名 |
| `order` | number | 否 | 排序序号，数字越小越靠前，未设置则按文件名排序 |

### 示例

```json
{
  "title": "React Hooks 最佳实践",
  "slug": "react-hooks-best-practices",
  "order": 2
}
```

## 📝 文档 Frontmatter

每个 Markdown 文档可以包含 frontmatter 定义元数据：

```markdown
---
title: useState 使用指南
slug: use-state-guide
order: 1
---

# useState 使用指南

## 基本用法

...
```

### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `title` | string | 否 | 文档标题，未设置则使用文件名 |
| `slug` | string | 否 | URL 路径片段，未设置则使用文件名（去扩展名） |
| `order` | number | 否 | 在所属分类下的排序，未设置则按文件名排序 |

## 🚀 同步到 Sanity

### 快速开始

1. **配置环境变量**（首次使用）

   ```bash
   cp .env.example .env
   # 编辑 .env 填写 Sanity 配置
   ```

2. **生成导入文件**

   ```bash
   yarn import:generate
   ```

3. **上传到 Sanity**

   ```bash
   yarn import:upload
   ```

详细说明请参考 [`scripts/README.md`](../scripts/README.md)

## 💡 最佳实践

### 1. 使用数字前缀控制顺序

```
docs-source/
├── 01-frontend-basics/
├── 02-react/
└── 03-typescript/
```

### 2. 分类命名规范

- 使用小写字母和连字符：`react-hooks`
- 避免中文目录名（会影响 URL）
- 保持简洁有意义

### 3. 文档内容组织

- 每个文档聚焦一个主题
- 使用清晰的标题层级（h1-h6）
- 添加代码示例和说明
- 适当添加图片和图表

### 4. 复用和引用

在 MDX 文档中可以引用其他文档：

```markdown
详见 [useState 使用指南](./use-state-guide.md)
```

## 🎯 示例内容

创建你的第一个知识库分类：

```bash
mkdir -p docs-source/react-basics/hooks
cd docs-source/react-basics
```

创建 `_meta.json`:

```json
{
  "title": "React 基础",
  "order": 1
}
```

创建 `hooks/useState.md`:

```markdown
---
title: useState Hook 详解
order: 1
---

# useState Hook 详解

## 简介

useState 是 React 提供的用于在函数组件中添加状态的 Hook。

## 基本用法

\`\`\`jsx
import { useState } from 'react';

function Counter() {
  const [count, setCount] = useState(0);
  
  return (
    <button onClick={() => setCount(count + 1)}>
      点击次数: {count}
    </button>
  );
}
\`\`\`

## 注意事项

- 状态更新是异步的
- 使用函数式更新避免闭包陷阱
- 不要在循环、条件或嵌套函数中调用 Hook
```

然后运行同步脚本即可将内容导入 Sanity！

## 📚 与博客文章的区别

| 特性 | Blog/ (博客随笔) | docs-source/ (知识库) |
|------|------------------|----------------------|
| 内容性质 | 随笔、总结、经验分享 | 系统性知识、教程、文档 |
| 组织方式 | 时间维度（年/月） | 主题维度（分类/子分类） |
| 更新频率 | 不定期新增 | 持续更新完善 |
| Sanity Schema | `post` | `docCategory` + `docPage` |
| 前端呈现 | 博客列表页 | VuePress 风格知识库 |

## ❓ 常见问题

### Q: 支持哪些 Markdown 语法？

A: 支持标准 Markdown 和 MDX 语法，包括：

- 标题、列表、引用
- 代码块（支持语法高亮）
- 表格
- 图片和链接
- React 组件（MDX）

### Q: 如何组织大型知识库？

A: 建议按技术栈分一级分类，按主题分二级分类：

```
frontend/
  react/
    basics/
    hooks/
    advanced/
  vue/
  typescript/
backend/
  node/
  database/
```

### Q: 文档更新后需要重新同步吗？

A: 是的，每次本地文档更新后需要运行 `yarn import:generate && yarn import:upload` 重新同步。

### Q: 可以删除分类或文档吗？

A: 可以在 Sanity Studio 中手动删除，或者使用 `--replace` 参数重新导入会覆盖现有数据。
