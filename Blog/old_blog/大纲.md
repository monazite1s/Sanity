> 记录一下后续想要主要学/写的点

## 前端基建

1. 前端规范
   - 代码规范：约束代码规范、风格
     - eslint、prettier、stylelint、tsLint
     - commitlint、husky
   - 团队约束
     - Vue开发手册 命名规范
     - 阿里巴巴前端规范手册
2. 前端文档（SSG 静态网站生成）
   1. vitepress、vuepress、wordpress
3. 前端项目模板
   1. Admin
   2. nest g res xxx 生成模板（Restful）
   3. ejs 模板（嵌入式模板引擎）
4. 前端工程化工具
   1. commander、prompt、chalk
   2. vite(bundleLess思想)  webpack  （打包项目，webpack之前的版本不支持esm require）
   3. babel   、 swc oxc(rust)、 esbuild （go，天然支持ts）
   4. rollup（打包库， 支持import require umc cmd amd iife）
   5. gulp（基于任务的打包器、 scss less css js 字体）
   6. grunt rspack
   7.  tsup tsc （打包ts）
   8. postcss（处理css tailwindcss属于postcss的插件）
   9. autoprefixer 补充浏览器前缀 -moz-xxx
   10. broswerlist 浏览器兼容性（数据来源caniuse）
   11. corejs - polyfill (重写不存在的方法)
5. 前端组件库
6. 前端工具库
   1. husky 自动部署 git hooks
7. 工具自动化
8. BFF
   1. 桥接层、中间层 Backends For Frontends
   2. 格式统一
   3. Nodejs 中间层写算法 集群 多线程。Node 高IO
   4. Node的IO是基于libuv实现的，libuv是c写的， Windows基于IOCP -> named pipe实现 Linux基于epoll -> socket domain实现
   5. Node薄弱：大数据计算、加密解密 crypto -> openSSL
9. CICD
   1. git Actions
   2. Jenkins CICD
10. 埋点监控
    1. 神策 友盟
11. 可视化
12. 性能优化
    1. 减少接口次数 图片base64 CDN
13. 低代码搭建
14. SSR
    1. 首次加载快（利好首屏）
    2. SEO优化、 robots.txt    TDK  meta title description keywords
15. 响应式涉及、CSS原子化
    1. Rem布局、PostCss(pxtoviewport)
    2. 媒体查询
    3. tailwind、unocss
16. 微前端
    1. iframe、webView
    2. wujie
    3. qiankun
    4. micro-app

## 跨部门复用组件

1. npm包、SDK
2. webpack 模块联邦技术
3. gitSubmodule子模块

# SSR

水合

Next架构

CMS+Next+客户端